---
title: Themes & Appearance
description: 11 built-in color themes, launcher icon theming, font size, and notebook-list display options.
---

import ThemeShot from '../../../components/ThemeShot.astro';
import settingsAppearanceLight from '../assets/light/settings-appearance.png';
import settingsAppearanceDark from '../assets/dark/settings-appearance.png';

Settings → Look and Feel is where Grove's visual presentation is configured, separate from note *content* settings like TODO keywords (see [Notes Behavior](/features/notes-behavior)).

<ThemeShot
  light={settingsAppearanceLight}
  dark={settingsAppearanceDark}
  alt="Look and Feel settings showing the Grove Light theme picker, Sync App Icon with Theme toggle, Show file icons in notebooks and Flatten folders toggles, a Filename/Title notebook display name control, and a Small/Medium/Large font size control"
  widths={[340, 680]}
  sizes="340px"
/>

## Themes

Grove ships 11 built-in palettes, picked from a dropdown:

**Light themes:** Grove Light, Tokyo Day, Catppuccin Latte, Rosé Pine Dawn
**Dark themes:** Grove Dark, Tokyo Night, Synthwave, Dracula, Catppuccin Mocha, Nord, Rosé Pine Moon

There's no "follow system" auto option; theme selection is always explicit. Each theme defines a full token set (background, surface, ink/text, accent, and semantic colors for green/amber/red/blue/violet), so every screen in the app (including org-syntax highlighting in the editor) reflects the chosen theme consistently.

## Launcher icon theming

The **Sync App Icon with Theme** toggle swaps the app's launcher icon (and the brand mark in the nav drawer) to match the active theme, using Android's `activity-alias` mechanism. Because of how icon aliasing works on Android, this requires the app to background and restart to take effect; it won't update the icon instantly while Grove is in the foreground. Note: the notification-shade icon can't follow the theme on Android 16+, a platform limitation rather than something Grove controls.

## Font size

A three-way segmented control: **Small / Medium / Large**, applied app-wide.

## Notebook list

- **Show file icons in notebooks**: whether each row of the [Notebooks](/features/notebooks) list carries its monogram tile. On by default; turn it off for a plainer, text-only list. The per-notebook color you pick is kept either way, it just isn't drawn while this is off.
- **Flatten folders**: hide the folder tree and list every note in one flat list, each note showing its folder path as a subtitle. Top-level notes look unchanged. The drill-down view and expand/collapse-all are hidden while this is on; you can still move a note between folders from its long-press menu. Off by default. See [Notebooks](/features/notebooks).
- **Notebook display name**: whether the list labels each notebook by its **Filename** or by its `#+TITLE:` (**Title**), falling back to the filename when a note has no title. The monogram letter follows the filename either way.

These are view-only; turning them off or switching them doesn't change anything on disk.

## Related

- [Notes Behavior](/features/notes-behavior): TODO keywords, priorities, and other content-level settings
- [Edit Mode](/features/edit-mode): where the syntax-highlighting colors from the active theme show up most
