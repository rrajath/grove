---
title: Notebooks
description: "The home screen: a list of your .org files as notebooks, with sync status, quick actions, and the navigation drawer."
---

import ThemeShot from '../../../components/ThemeShot.astro';
import notebooksLight from '../assets/light/notebooks.png';
import notebooksDark from '../assets/dark/notebooks.png';
import navDrawerLight from '../assets/light/nav-drawer.png';
import navDrawerDark from '../assets/dark/nav-drawer.png';

Notebooks is Grove's home screen. Every `.org` file in your configured sync folder shows up here as a notebook card; there's no separate "import" step, no database to seed. If a `.org` file exists in the folder, it's a notebook.

<ThemeShot
  light={notebooksLight}
  dark={notebooksDark}
  alt="Notebooks list showing six notebooks (ideas.org, inbox.org, journal.org, reading.org, recipes.org, travel.org) with monogram icons, note counts, last-modified times, a Conflict pill on journal.org, and a Capture FAB"
  widths={[340, 680]}
  sizes="340px"
/>

## What each card shows

Every notebook card displays:

- **Monogram icon**: a small colored tile showing the first letter of the notebook's name (its `#+TITLE:` when the display name is set to "Title", otherwise the filename). The color is customizable per notebook (long-press → *Change icon color*); hide the tiles list-wide with **Show file icons in notebooks** in Settings → Notebooks.
- **Notebook name**: the filename by default, or the file's `#+TITLE:` if you've set the notebook display name to "Title" in Settings → Notebooks
- **Note count and last-modified time**: e.g. "9 notes · 21 hours ago"
- **Sync status pill**: appears when a notebook is mid-sync, freshly modified, or has a conflict

## Top bar actions

| Icon | Action |
|---|---|
| ☰ | Open the navigation drawer |
| + | Create a new notebook |
| ✓ | Shows status and timestamp of the last sync |
| 🔍 | Open Search |

## Long-press menu

Long-pressing a notebook card opens a context menu with:

- **Pin to top** / **Unpin**: keep frequently used notebooks at the top of the list, independent of alphabetical or recency order. Pinned notebooks and folders share one Pinned strip, listed in the order you pinned them
- **Rename**
- **Change icon color**: pick the tile color for this notebook's monogram from the theme palette
- **Force reload**: re-parse this file from disk, bypassing the normal change-detection diff (useful if you suspect Grove's index is stale)
- **Resolve conflict**: jumps straight to the [conflict resolution](/features/conflict-resolution) screen when a Syncthing conflict copy exists for this file
- **Delete**: see below

### Deleting a notebook is non-destructive

Deleting a notebook doesn't remove the file. Grove renames it to `<name>.org.trash`, so it drops out of the notebook list but stays on disk, fully recoverable with any file browser or your sync tool. There's no in-app "restore from trash"; you get the file back by renaming it back to `.org` outside the app.

### Folder rows

Subfolders in your sync directory show up as folder rows in the list. Long-pressing one opens its own menu:

- **Pin to top** / **Unpin**: a pinned folder moves out of the tree and into the Pinned strip at the top of the list, taking everything inside it along. It shares that strip with pinned notebooks, in the order things were pinned. Tap it to expand its contents in place; a folder big enough to open as its own screen drills in instead
- **Rename**: renames the folder and moves every `.org` file inside it, keeping each file's icon color and pinned position
- **Change icon color**: pick the folder tile's color from the theme palette
- **Delete**: moves every `.org` file inside the folder to the trash, the same non-destructive rename as deleting a single notebook. An empty folder simply stops showing once its last file is gone.

### Flat list

**Settings → Notebooks → Flatten folders** collapses the tree: folder rows disappear and every note shows as its own row, with its folder path (`projects/clients/`) as a small subtitle. Notes at the top level look unchanged. The monogram letter still comes from the filename, not the folder. While flat mode is on, the drill-down view and expand/collapse-all are hidden, and **+** always creates at the vault root, but you can still move a note between folders from its long-press menu. Pinned notes and pinned folders' contents still show in the Pinned strip, as a flat list.

## Creating a notebook

Tap **+** and give the new file a name. Grove creates an empty `.org` file in your sync folder immediately.

## Settings

**Settings → Notebooks** collects everything that shapes this list. All of it is view-only; nothing here changes anything on disk.

- **Show file icons in notebooks**: whether each row carries its monogram tile. On by default; turn it off for a plainer, text-only list. The per-notebook color you pick is kept either way, it just isn't drawn while this is off.
- **Flatten folders**: hide the folder tree and list every note in one flat list, each note showing its folder path as a subtitle (see [Flat list](#flat-list) above). Off by default.
- **Notebook display name**: whether the list labels each notebook by its **Filename** or by its `#+TITLE:` (**Title**), falling back to the filename when a note has no title. The monogram letter follows the filename either way.

## Navigation drawer

Tap the hamburger icon (☰) to open the drawer:

<ThemeShot
  light={navDrawerLight}
  dark={navDrawerDark}
  alt="Navigation drawer showing the Grove brand header with vault path, Search, Notebooks (highlighted), Saved Searches (Scheduled Today, All TODO, This Week), Agenda, and Settings links"
  widths={[340, 680]}
  sizes="340px"
/>

The drawer shows:

- **Vault path**: the folder Grove is currently synced to, under the app name
- **Search**: jumps to the [Search](/features/search) screen
- **Notebooks**: the current screen, highlighted
- **Saved Searches**: your starred searches for one-tap access (see [Search](/features/search))
- **Agenda**: jumps to [Agenda](/features/agenda)
- **Settings**: jumps to the [Settings hub](/features/settings-overview)

## Deep links

Grove registers `grove://` deep links so widgets, notifications, and shortcuts can jump straight into the app:

- `grove://note/{id}`: opens a specific note
- `grove://capture`: opens the capture picker
- `grove://agenda`: opens Agenda
- `grove://reminder/...`: used internally by reminder notifications

## Related

- [Outline View](/features/outline-view): what you see after opening a notebook
- [Sync](/features/sync): how the sync status pills and re-index behavior work
- [Conflict Resolution](/features/conflict-resolution): resolving a Syncthing conflict copy
