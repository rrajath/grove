---
---

<div id="features" class="features-heading">
  <div class="features-heading__title"><span class="tok-green">*</span> Features</div>
  <div class="features-heading__hint"># TAB folds a headline. It works here too, go on.</div>
</div>

<style>
  .features-heading {
    padding: 56px 56px 20px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .features-heading__title {
    font-family: var(--font-mono);
    font-size: 26px;
    font-weight: 700;
    color: var(--ink);
  }
  .features-heading__hint {
    font-family: var(--font-mono);
    font-size: 12.5px;
    color: var(--ink3);
  }

  @media (max-width: 700px) {
    .features-heading {
      padding: 24px 24px 16px;
    }
  }
</style>
