# Robolectric runs integration tests against SDK 34 shadows (the app's minSdk).
# Only tests annotated @RunWith(RobolectricTestRunner::class) load this; the
# pure-logic JVM suite is unaffected.
sdk=34
