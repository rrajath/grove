Add 2-8 PNG or JPEG screenshots here before submitting to F-Droid (e.g. `1.png`, `2.png`, ...).

F-Droid reads this `fastlane/metadata/android/en-US/` structure directly from the app's own repository, so screenshots placed here become part of the store listing with no extra upload step.

These need to be captured from a real running build (debug or release) against sample vault content, not the maintainer's personal vault. This file is a placeholder — delete it once real screenshots are added.
