*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.impeccable

.kotlin

# Signing material — never commit keystores or their base64; CI pulls these
# from repo secrets (see .github/workflows/build.yml).
*.keystore
*.jks

PROGRESS.md
.understand-anything

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Internal readiness/compliance reports — not for public distribution.
internal-docs/

# Local agent tooling — not shared via the repo.
.claude
.ignore
.mcp.json
.serena

# Working docs for the android-dev-workflow skill (plans, audits, learnings).
internal/
